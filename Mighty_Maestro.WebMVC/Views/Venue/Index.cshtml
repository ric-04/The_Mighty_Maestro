@model IEnumerable<Mighty_Maestro.Models.Venue.VenueListItem>

@{
    ViewBag.Title = "Index";
}

<div style="background-color:white;padding:10px;opacity:70%;">
    <p style="font-size:25px;">
        @Html.ActionLink("Create New", "Create")
    </p>
    <h2 class="text-center">Venue Index</h2>
    <table class="table">
        <tr style="color:red;font-size:22px;">
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>

                @Html.DisplayNameFor(model => model.VenueId)
            </th>

            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.HiddenFor(modelItem => item.VenueId)

                </td>
                <td>
                    @*@Html.ActionLink("Edit", "Edit", new { id = item.VenueId }) |*@
                    @Html.ActionLink("Details", "Details", new { id = item.VenueId })
                    @*@Html.ActionLink("Delete", "Delete", new { id = item.VenueId })*@
                </td>
            </tr>
        }

    </table>
</div>